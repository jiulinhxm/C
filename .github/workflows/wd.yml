name: write good
on: push

permissions: {}

jobs:
  write-good:
    runs-on: ubuntu-latest
    steps:
      - run: npm install -g write-good
      - run: npm install glob
  
      - run: write-good $(test.md)
